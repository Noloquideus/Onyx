name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.11]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH
      shell: bash
    
    - name: Install dependencies
      run: |
        export PATH="$HOME/.local/bin:$PATH"
        poetry lock
        poetry install
      shell: bash
    
    - name: Install PyInstaller
      run: |
        export PATH="$HOME/.local/bin:$PATH"
        poetry run pip install pyinstaller
      shell: bash
    
    - name: Build executable
      run: |
        export PATH="$HOME/.local/bin:$PATH"
        poetry run pyinstaller --onefile --name onyx onyx/main.py
        if [ "${{ runner.os }}" == "Windows" ]; then
          mv dist/onyx.exe dist/onyx-windows.exe
        else
          mv dist/onyx dist/onyx-linux
        fi
      shell: bash
    
    - name: Upload executable
      uses: actions/upload-artifact@v4
      with:
        name: onyx-${{ runner.os }}
        path: dist/onyx*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          onyx-linux/onyx-linux
        body: |
          ## 🎉 Onyx CLI v${{ github.ref_name }}
          
          ### 📦 Downloads
          - **Linux**: `onyx-linux` - Сделайте исполняемым: `chmod +x onyx-linux`
          
          ### 🚀 Features
          - 🌳 **tree**: Отображение структуры папок с размерами и временем
          - 📊 **count**: Подсчет строк в файлах с детальной статистикой
          - 🔍 **find**: Умный поиск файлов и содержимого
          - 💾 **backup**: Создание и управление резервными копиями
          - 📈 **git**: Анализ Git репозиториев
          - 🌐 **net**: Сетевые утилиты и диагностика
          - ⬇️ **download**: Загрузка файлов с прогресс-барами
          - 📊 **monitor**: Мониторинг системных ресурсов
          
          ### 📋 Usage
          ```bash
          # Linux
          ./onyx-linux --help
          ```
          
          ### 🔧 Installation
          1. Скачайте файл для вашей ОС
          2. Для Linux: `chmod +x onyx-linux`
          3. Переместите в папку из PATH или используйте полный путь
          
          ### 📖 Examples
          ```bash
          # Показать дерево папок
          onyx tree --show-size
          
          # Подсчитать строки в Python файлах
          onyx count --extensions .py --show-files
          
          # Мониторинг системы
          onyx monitor system
          ```
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # Если нужно, используйте PAT: GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
